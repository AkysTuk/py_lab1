from math_utils import factorial, sum_even_numbers

# Використання функції factorial
num = 5
print(f"Факторіал числа {num} дорівнює {factorial(num)}")

# Використання функції sum_even_numbers
numbers = [1, 2, 3, 4, 5, 6]
print(f"Сума парних чисел у списку {numbers} дорівнює {sum_even_numbers(numbers)}")